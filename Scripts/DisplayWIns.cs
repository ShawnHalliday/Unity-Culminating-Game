using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
public class DisplayWIns : MonoBehaviour
{
    public TMP_Text wincountText;
    private void Start()
    {
        
    }
    // Update is called once per frame
    void Update()
    {
        //wincountText.text = wincount._wincount.ToString();
    }
}
